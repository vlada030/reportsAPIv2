extends layouts/main-layout.pug

block primaryNavBar
    include layouts/primaryNavBarAll.pug

block secondaryNavBar
    include layouts/secondaryNavBar.pug
        block secondaryMenu
            include layouts/secondaryMenuReport.pug

block content
    .container.my-3.w-100
        .row.w-100
            .list-group.w-100
                each report in reports
                    a(href=`../dom/${report.MISBroj}`).list-group-item.list-group-item-action.d-flex.justify-content-around
                        span=`Radni Nalog: ${report.radniNalog}` 
                        span=`MIS Broj: ${report.MISBroj}`
                        span=`Naziv Proizvoda: ${report.proizvod.proizvod}`
                        span=`Dužina: ${report.duzina}m`
    
block footer    
    include layouts/fixed_footer.pug